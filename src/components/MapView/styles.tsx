import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    mapContainer: {
        flex: 1,
        paddingTop: 10
    },
    map: {
        flex: 1,
        borderRadius: 8,
    },
});

export default styles;